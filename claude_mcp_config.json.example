{
  "_comment": "Example Claude Code MCP config. Easier: run `python scripts/register_mcp.py --client claude-code` and it will write the right entry into ~/.claude/settings.json automatically. This file is here for reference if you want to register by hand.",
  "mcpServers": {
    "jarvis-memory": {
      "command": "<JARVIS_MEMORY_ROOT>/.venv/bin/jarvis-mcp",
      "args": [],
      "cwd": "<JARVIS_MEMORY_ROOT>"
    }
  }
}
